// typescript deep dive book
// https://github.com/basarat/typescript-book

// compile from CLI: npm run build (will compile on each save)

import helper from './helper.js'; // use .js or .cjs extension for imports

const output = helper('It\'s working!');
console.log(output);